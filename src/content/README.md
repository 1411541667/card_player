# Content packs

`wasteland/` is the active first-version pack. Its `data/` JSON files are the runtime source and its
`tables/` CSV files are the human-readable design mirror. Additional packs can be assembled through
the same `ContentPack` interface and selected in `src/main.tsx`.
